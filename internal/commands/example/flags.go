package example

import "changelog-generator/internal/types/output"

var (
	name   string        // an example named-argument
	format = output.JSON // setting to a constant provides a default, valid value
)
